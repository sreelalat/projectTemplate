const activityState = {
  IDLE: "idle",
  SILENTCHECK: "silent check",
  LOADING: "loading",
  WAIT: "wait",
  SUCCESS: "success",
  ERROR: "error",
};

const viewer = "NCS";

export { activityState, viewer };
